[pytest]
testpaths = tests
markers =
    corpus: read-only run against the real game corpus (needs --corpus)
    slow: slow test, opt in with --bench (or -m slow)
    bench: LuaJIT microbenchmark harness (tools/microbench.py, bench/)
